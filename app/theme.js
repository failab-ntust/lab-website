'use client';
import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
    palette: {
        primary: {
            light: '#fddf33',
            main: '#FDD700',
            dark: '#b19600',
        },
        secondary: {
            light: '#4383cc',
            main: '#1565C0',
            dark: '#0e4686'
        }
    },
});